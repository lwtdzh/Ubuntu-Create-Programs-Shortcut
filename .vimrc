" Open the line number show.
set number

" Grammer highlight.
syntax on

" Show if it is on command or insert Mode at the button.
set showmode

" Show current unfinished command at command Mode.
set showcmd

" Use UTF-8.
set encoding=utf-8

" Auto indent after type ENTER.
set autoindent

" Tab length.
set tabstop=4
set shiftwidth=4

" Convert TAB to SPACE.
set expandtab
set softtabstop=4

" Highlight the current line.
set cursorline

" Highlight the matched syntax.
set showmatch

" Highlight the search result.
set hlsearch

" Dynamic search (Type a char and auto match).
set incsearch

